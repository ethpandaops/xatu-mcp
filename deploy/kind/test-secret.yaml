# Test secret for KIND deployment
# Uses placeholder values for testing
apiVersion: v1
kind: Secret
metadata:
  name: ethpandaops-mcp-secrets
  namespace: ethpandaops-mcp
  labels:
    app.kubernetes.io/name: ethpandaops-mcp
    app.kubernetes.io/component: secrets
type: Opaque
stringData:
  # Base URL
  MCP_BASE_URL: "http://localhost:30000"

  # Placeholder ClickHouse (not used in basic tests)
  CLICKHOUSE_XATU_HOST: "clickhouse.example.com:8443"
  CLICKHOUSE_XATU_DATABASE: "default"
  CLICKHOUSE_XATU_USERNAME: "test"
  CLICKHOUSE_XATU_PASSWORD: "test"

  # Placeholder Prometheus
  PROMETHEUS_URL: "https://prometheus.example.com"
  PROMETHEUS_USERNAME: "test"
  PROMETHEUS_PASSWORD: "test"

  # Placeholder Loki
  LOKI_URL: "https://loki.example.com"
  LOKI_USERNAME: "test"
  LOKI_PASSWORD: "test"

  # MinIO S3 (running in cluster)
  S3_ENDPOINT: "http://minio.ethpandaops-mcp.svc:9000"
  S3_ACCESS_KEY: "minioadmin"
  S3_SECRET_KEY: "minioadmin"
  S3_PUBLIC_URL: "http://localhost:30001"
